// 08.暗号文
function ciphe(str: string): void {
  for (let i = 0; i < str.length; i++) {
    let unicode: number;

    // // unicode = 219 - sentence.codePointAt(i);
    // unicode
    // for (let j = 0; j < str.length; j++) {
    //   let encrypt = String.fromCharCode(unicode);
    //   //   process.stdout.write(encrypt);
  }
}

// }
function dec(str: string) {
  for (let i = 0; i < str.length; i++) {
    let unicode: number;

    // unicode = cipher(sentence).codePointAt(i) + 219;
    // console.log(unicode);
    // for (let j = 0; j < str.length; j++) {
    //   let dencryption = String.fromCharCode(unicode);
    //   process.stdout.write(dencryption);
  }
}
// }
let sentence = "I can take my time though.";
// const splitSentence = sentence.split('').reduce();
// const getEncrypt = cipher(sentence);
// process.stdout.write(getEncrypt);
